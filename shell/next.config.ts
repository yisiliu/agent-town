import type { NextConfig } from 'next';

const config: NextConfig = {
  reactStrictMode: true,
  // Next defaults to looking for pages under src/ when src/app exists,
  // so no extra option is required here.
};

export default config;
